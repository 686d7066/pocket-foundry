{
  "name": "pocket-foundry",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "clean": "node --experimental-transform-types scripts/build.ts --clean",
    "build": "node --experimental-transform-types scripts/build.ts",
    "pr:comments": "node --experimental-transform-types scripts/pr-unresolved-comments.ts",
    "check:unused": "node node_modules/typescript/bin/tsc --noEmit --noUnusedLocals --noUnusedParameters",
    "test": "npm run check:unused && vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "@types/node": "^22.15.0",
    "esbuild": "^0.25.0",
    "tailwindcss": "^4.3.0",
    "typescript": "^5.8.0",
    "vitest": "^4.0.8"
  }
}
